% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shapeinfCode.R
\name{shapeinf}
\alias{shapeinf}
\title{Shape-Restricted Inference for Treatment Effects}
\usage{
shapeinf(
  data,
  target.parameter,
  alpha = 0.05,
  bases,
  beta.null,
  number.bootstraps = 500,
  bootstrap.seeds = NULL,
  gamma.lambda.df = NULL,
  kappa = NULL,
  MB = 1,
  dgp.in = NULL,
  parallel = TRUE,
  return.gurobi = FALSE,
  gurobi.tries = 50,
  late.lb = NULL,
  late.ub = NULL
)
}
\arguments{
\item{data}{A data frame containing columns \code{y} (outcome), \code{d} (treatment), and \code{z} (instrument).}

\item{target.parameter}{The treatment effect parameter to estimate (e.g., ATT, ATE, LATE).}

\item{alpha}{Significance level for confidence intervals (default: 0.05).}

\item{bases}{A list of basis functions used in the estimation.}

\item{beta.null}{A vector of null values for the hypothesis test.}

\item{number.bootstraps}{Number of bootstrap samples (default: 500).}

\item{bootstrap.seeds}{Optional vector of bootstrap seeds.}

\item{gamma.lambda.df}{Optional data frame of gamma-lambda values.}

\item{kappa}{Regularization parameter (if \code{gamma.lambda.df} is not provided).}

\item{MB}{Bootstrap method indicator (default: 1).}

\item{dgp.in}{Optional; data-generating process specification. If included, computes population \eqn{\tau} rather than estimated \eqn{\tau}.}

\item{parallel}{Logical; whether to use parallel computation (default: TRUE).}

\item{return.gurobi}{Logical; whether to return raw Gurobi output (default: FALSE).}

\item{gurobi.tries}{Number of attempts for Gurobi solver (default: 50).}

\item{late.lb}{Lower bound for LATE estimation (required when target.parameter = "LATE")}

\item{late.ub}{Upper bound for LATE estimation (required when target.parameter = "LATE")}
}
\value{
An object of class \code{shapeinf} containing:
\itemize{
\item \code{ci.bounds}: Confidence interval for the target parameter.
\item \code{beta.null.test.detailed}: Detailed test statistics for \code{beta.null} values.
\item \code{bootstrap.detailed}: Detailed bootstrap test statistics.
\item \code{data}: The input data.
\item \code{bases}: The basis functions used.
\item \code{tau}: Computed tau values.
\item \code{C0}, \code{C1}: Computed C0 and C1 matrices.
}
}
\description{
This function performs shape-restricted inference for a given target parameter.
}
